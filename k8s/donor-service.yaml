apiVersion: apps/v1
kind: Deployment
metadata:
  name: donor-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: donor-service
  template:
    metadata:
      labels:
        app: donor-service
    spec:
      containers:
      - name: donor-service
        image: bloodbridge-donor:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 3002
        env:
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: bloodbridge-secrets
              key: DATABASE_URL
        - name: REDIS_URL
          valueFrom:
            secretKeyRef:
              name: bloodbridge-secrets
              key: REDIS_URL
        livenessProbe:
          httpGet:
            path: /health
            port: 3002
          initialDelaySeconds: 15
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 3002
          initialDelaySeconds: 5
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: donor-service
spec:
  selector:
    app: donor-service
  ports:
    - port: 3000
      targetPort: 3002
      nodePort: 30002
  type: NodePort
