#!/bin/sh
echo "🧪 Running tests before push..."
./test-all.sh
if [ $? -ne 0 ]; then
    echo "❌ Tests failed! Push blocked. Fix coverage first."
    exit 1
fi
echo "✅ All tests passed! Pushing to GitHub..."
